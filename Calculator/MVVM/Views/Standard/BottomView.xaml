<UserControl x:Class="Calculator.MVVM.Views.Standard.BottomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             DataContext="{Binding Source={StaticResource locator}, Path=BottomViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <UserControl.Resources>
        <!-- ListBoxItem style so selection and hover follow theme -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="4 2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="6">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryHueLightBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryHueLightBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Simple empty-state text style -->
        <Style x:Key="EmptyStateText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAccentBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="12" />
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="Transparent" Padding="8" Margin="0,6,0,6">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock x:Name="txt" Text="History" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center" />
                    <materialDesign:Badged Badge="{Binding OperationVM.Nbr}" Margin="10,0,0,0" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="History" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="20" Height="20" />
                    </materialDesign:Badged>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding DeleteAllCommand}" ToolTip="Clear all history" Focusable="False">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TrashCan" />
                            <TextBlock Text=" Clear" Margin="6,0,0,0" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Card with history list -->
        <Border Grid.Row="1" CornerRadius="12" Padding="12" Background="{DynamicResource MaterialDesignCardBackground}" materialDesign:ElevationAssist.Elevation="Dp6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                </Grid.RowDefinitions>

                <ListBox ItemsSource="{Binding OperationVM.Items}"
                         x:Name="listbox"
                         SelectedItem="{Binding OperationVM.SelectedItem, Mode=TwoWay}"
                         Background="Transparent"
                         
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding OpValue}" TextWrapping="Wrap" Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" FontSize="13" />
                                    <TextBlock Text="{Binding OpResult}" Foreground="{DynamicResource PrimaryHueMidBrush}" FontWeight="Bold" FontSize="18" />
                                </StackPanel>

                                <!-- Optional: a small chevron or action on the right -->
                                <materialDesign:PackIcon Grid.Column="1" Kind="ChevronRight" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.Template>
                        <ControlTemplate TargetType="ListBox">
                            <Border Background="Transparent">
                                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </ListBox.Template>

                </ListBox>

                <!-- Empty state -->
                <TextBlock Grid.Row="0" Text="No history yet" Style="{StaticResource EmptyStateText}" Visibility="{Binding OperationVM.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>

    </Grid>
</UserControl>
