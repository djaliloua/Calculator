<UserControl x:Class="Calculator.MVVM.Views.Standard.About"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Calculator.MVVM.Views.Standard"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="480">
    <Grid>
        <Border CornerRadius="12" Padding="24" Background="White" Opacity="0.98" 
                materialDesign:ElevationAssist.Elevation="Dp6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Header with app icon and title -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="0">
                    <Border Width="72" Height="72" CornerRadius="36" Background="#FF2A5298" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="Calculator" Width="40" Height="40" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Calculator" FontSize="22" FontWeight="Bold" Foreground="#0F1724" />
                        <TextBlock Text="Fast. Beautiful. Reliable." FontSize="12" Foreground="#7A8496" />
                    </StackPanel>
                </StackPanel>

                <!-- Short description and version -->
                <StackPanel Grid.Row="1" Margin="0,18,0,0">
                    <TextBlock TextWrapping="Wrap" Foreground="#263238" FontSize="13">
                        This is a modern calculator built with Material Design for WPF. It combines a clean UI with powerful features for everyday calculations.
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Version:" FontWeight="SemiBold" Foreground="#263238" />
                        <TextBlock Text=" 1.0.0" Foreground="#455A64" />
                    </StackPanel>
                </StackPanel>

                <!-- Content: links, author, license -->
                <Grid Grid.Row="2" Margin="0,18,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="About this project" FontWeight="SemiBold" FontSize="14" Foreground="#263238" />
                        <TextBlock TextWrapping="Wrap" Margin="0,6,0,0" Foreground="#455A64">
                            Built as a sample desktop application demonstrating MVVM, dependency injection and Material Design styling. Contributions and feedback are welcome.
                        </TextBlock>

                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Account" Width="20" Height="20" Foreground="#2A5298" />
                            <TextBlock Text="  Author: djaliloua" VerticalAlignment="Center" Foreground="#455A64" Margin="8,0,0,0" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Open project on GitHub" Click="OpenGitHub_Click">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Github" Width="18" Height="18" />
                                    <TextBlock Text="  View on GitHub" />
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource MaterialDesignFlatButton}" Margin="12,0,0,0" Click="OpenLicense_Click">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileDocument" Width="18" Height="18" />
                                    <TextBlock Text="  License" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <!-- Decorative panel on right -->
                    <StackPanel Grid.Column="1" Width="160" Margin="18,0,0,0" HorizontalAlignment="Right">
                        <Border Background="#FFF5F7FF" CornerRadius="8" Padding="12" HorizontalAlignment="Stretch">
                            <StackPanel>
                                <TextBlock Text="Powered by" FontSize="12" Foreground="#62727B" />
                                <TextBlock Text="Material Design In XAML" FontWeight="Bold" Foreground="#2A5298" />
                            </StackPanel>
                        </Border>

                        <Border Background="#FFF7F9FB" CornerRadius="8" Padding="12" Margin="0,12,0,0">
                            <StackPanel>
                                <TextBlock Text="License" FontSize="12" Foreground="#62727B" />
                                <TextBlock Text="MIT" FontWeight="Bold" Foreground="#455A64" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>